<div class="container">
  <%= simple_form_for @post do |f| %>
    <%= f.input :photo, as: :file %>
    <%= f.input :caption %>
    <%= f.submit 'Post', class: 'primary-button', data: { action: 'click->common#showLoader' } %>
  <% end %>
</div>
